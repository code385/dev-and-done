export const metadata = {
  title: 'Blogs',
  description: 'Read our latest articles, insights, and thoughts on web development, mobile apps, AI, and technology.',
};

export default function BlogsLayout({ children }) {
  return children;
}

